.col-md-4
  %h2 
    -case elem.status
    -when elem.FINISHED
      .status_finished
    -when elem.STARTED
      .status_started
    -else
      .status_default
        =elem.name
  %p =elem.desc
  %ol
    -elem.project_subelement.each do |sub|
      =render '_subelement', locals: {elem: sub}

