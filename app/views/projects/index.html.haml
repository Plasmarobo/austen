.row
  %h1 Current projects:
.row
  .col-md-6
    - @projects.each do |project|
      = render partial:'project_fragment', locals: {project: project}
    %br
    = link_to 'New Project', new_project_path
